## 🛠 개발 시작 방법

```bash
npm install
npm run init:mock     # mock 데이터 초기화 (db.json 생성) 단, db.example.json은 훼손 금지! (원본)
npm run dev:json      # mock 서버 + Vite 개발 서버 실행
npm run dev:backend   # 스프링 부트 연동용 서버 실행
```
